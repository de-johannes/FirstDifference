name: Test CI (non-main branches)

on:
  push:
    branches:
      - '**'
      - '!main'  # Exclude main (release-ci handles that)
    paths:
      - 'DRIFE.agda'
      - 'src/python/*.py'
      - 'src/agda/**/*.agda'
      - '.github/workflows/test-ci.yml'

permissions:
  contents: read

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Agda
        uses: wenkokke/setup-agda@v2
        with:
          agda-version: '2.7.0.1'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: pip install numpy

      - name: Compile DRIFE.agda
        run: |
          echo "⏳ Compiling DRIFE.agda (--safe --without-K --no-libraries)..."
          agda --safe --without-K --no-libraries DRIFE.agda
          echo "✅ DRIFE.agda compiled successfully!"

      - name: Check for holes
        run: |
          echo "🔍 Checking for holes {!!}..."
          if grep -n '{!.*!}' DRIFE.agda; then
            echo "❌ Holes found!"
            exit 1
          else
            echo "✅ No holes found!"
          fi

      - name: Run K₄ validation
        run: |
          echo "🧮 Running validate_K4.py..."
          python src/python/validate_K4.py
          echo "✅ K₄ tests passed!"

      - name: Run α validation
        run: |
          echo "🧮 Running validate_alpha.py..."
          python src/python/validate_alpha.py
          echo "✅ α tests passed!"

      - name: Run Λ validation
        run: |
          echo "🧮 Running validate_lambda.py..."
          python src/python/validate_lambda.py
          echo "✅ Λ tests passed!"

      - name: Run cosmic age validation
        run: |
          echo "🧮 Running validate_cosmic_age.py..."
          python src/python/validate_cosmic_age.py
          echo "✅ Cosmic age tests passed!"

      - name: Summary
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          if [ "${{ job.status }}" == "success" ]; then
            echo "🎉 Test CI passed!"
            echo "   ✅ DRIFE.agda compiles (--safe --without-K)"
            echo "   ✅ No holes detected"
            echo "   ✅ K₄, α, Λ, cosmic age validations"
          else
            echo "❌ Test CI failed!"
          fi
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
